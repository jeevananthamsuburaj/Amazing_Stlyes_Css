<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Frying Pan Loader</title>

<style>
body{
    margin:0;
    height:100vh;
    background:#1e1e1e;
    display:flex;
    justify-content:center;
    align-items:center;
}

/* Container */
.loader{
    position:relative;
    width:180px;
    height:180px;
    animation: tilt 1.4s infinite ease-in-out;
}

/* Pan shape */
.pan{
    width:140px;
    height:140px;
    background:#3b3b3b;
    border-radius:50% 50% 45% 45%;
    position:absolute;
    left:20px;
    top:20px;
    box-shadow:
        inset 0 0 20px #000,
        0 8px 15px rgba(0,0,0,.5);
}

/* Handle */
.handle{
    width:110px;
    height:28px;
    background:#444;
    border-radius:15px;
    position:absolute;
    right:-100px;
    top:60px;
    transform:rotate(5deg);
    box-shadow:0 4px 10px rgba(0,0,0,.4);
}

/* Egg */
.egg{
    position:absolute;
    width:85px;
    height:60px;
    background:#ffffff;
    border-radius:50px 50px 45px 45px;
    left:27px;
    top:38px;
    animation:sizzle 1s infinite ease-in-out alternate;
}

/* Yolk */
.yolk{
    width:32px;
    height:32px;
    background:#ffce2e;
    border-radius:50%;
    position:absolute;
    top:14px;
    left:26px;
    box-shadow:inset 0 0 8px rgba(0,0,0,.2);
    animation:pulse .6s infinite ease-in-out alternate;
}

/* Sizzling bubbles */
.bubble{
    width:8px;
    height:8px;
    background:#fff;
    border-radius:50%;
    position:absolute;
    opacity:.6;
    animation:pop 0.7s infinite ease-in-out;
}
.b1{ left:20px; top:62px; animation-delay:.0s; }
.b2{ left:60px; top:25px; animation-delay:.2s; }
.b3{ left:55px; top:75px; animation-delay:.3s; }

/* -------- Animations -------- */

@keyframes tilt{
    0%,100%{ transform:rotate(0deg); }
    50%{ transform:rotate(3deg); }
}

@keyframes sizzle{
    0%{ transform:translateY(0); }
    100%{ transform:translateY(-3px); }
}

@keyframes pulse{
    0%{ transform:scale(1); }
    100%{ transform:scale(1.12); }
}

@keyframes pop{
    0%{ transform:scale(0.4); opacity:0.3; }
    50%{ transform:scale(0.9); opacity:0.8; }
    100%{ transform:scale(0.4); opacity:0.3; }
}
</style>
</head>

<body>

<div class="loader">
    <div class="pan"></div>
    <div class="handle"></div>

    <div class="egg">
        <div class="yolk"></div>
    </div>

    <!-- Bubbles -->
    <div class="bubble b1"></div>
    <div class="bubble b2"></div>
    <div class="bubble b3"></div>
</div>

</body>
</html>
